# MER-44 gate skip-integrity manifest.
#
# Format (whitespace-separated columns):
#   armed  build_tags  package_import_path  test_name_regex
#
# armed=yes  → CI fails if go test -json reports any Action=skip for that test.
# armed=no   → tracked for visibility; skips are allowed until blockers land.
#
# Flip armed to yes only when the gate's upstream tickets are merged and the
# suite is green on the 5.15 target. A gate may be declared green only with
# armed=yes and zero skips (see docs/PHASE1_GATES.md).

# P1.1 — MER-18 verdict matrix ≡ reference evaluator
yes bpf ./test/bpf/... TestVerdictMatrixMatchesReferenceEvaluator

# P1.2 — MER-29 live policy integration (agent stub product path)
yes integration ./test/integration/... TestLivePolicyIntegrationGate_MER29

# P1.3 — MER-21 Geneve two-node (underlay ingress + remote identity policy)
yes integration ./test/integration/... TestGeneveIngressIdentityPolicyGate_MER21

# CP-2 — MER-24 compiler ≡ reference property harness
yes '' ./internal/control/... TestCompilerMatchesReferenceProperty
yes '' ./internal/control/... TestConformanceRegressionCorpus

# O-2 — MER-32 denied-flows metrics gate
yes integration ./test/integration/... TestDeniedFlowsMetricsGate_MER32

# --- Phase 2 gates (MER-46 plan; implementation gated on MER-34) ---

# P2.1-N — MER-49 SOCKMAP permanent negative (CC-5 / eBPF R2)
yes bpf ./test/bpf/... TestSockmapNegativeGate_MER49

# P2.2 — MER-51 byte integrity + denied never redirected
yes integration ./test/integration/... TestSockmapIntegrityGate_MER51

# CP-3 — MER-56 ADS conformance + <500 ms propagation
yes '' ./internal/control/ads/... TestADSConformanceGate_MER56
