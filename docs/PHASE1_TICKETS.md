# Phase 1 Implementation Tickets — TC Policy Engine

Derived from [PHASE1_PLAN.md](PHASE1_PLAN.md) (same IDs). Format follows
[PHASE0_TICKETS.md](PHASE0_TICKETS.md): every ticket ≤ 4h, acceptance criteria
are testable, **Wave** marks tickets that run concurrently (different lanes
never block each other; within a lane, follow Deps). Owner roles per the plan:
eBPF · Agent · Platform · Obs.

| ID | Ticket | Owner | Est | Acceptance criteria | Deps | Files expected to change | Wave |
|----|--------|-------|-----|---------------------|------|--------------------------|------|
| MER-14 | **Phase 1 contract freeze** | eBPF + Agent (pair) | 3–4h | `policy_key` (with `direction` byte, D12), `policy_verdict`, `flow_key`, `deny_info` defined in `meridian_types.h` with `_Static_assert` on every size; `identity_map`/`policy_map`/`denied_flows_map` in `meridian_maps.h` per D14 schemas, all `LIBBPF_PIN_BY_NAME`; `MERIDIAN_SCHEMA_VERSION` = 2; `wire.PolicyRuleKey` gains `Direction uint8`; `gen.go` lists every new struct in `-type`; `make ebpf` regenerates clean and bindings committed; ADR-0003 records D12 with the rejected alternative | Phase 0 exit (MER-7/8/10 green) | `bpf/include/meridian_types.h`, `bpf/include/meridian_maps.h`, `bpf/gen.go`, `pkg/wire/policy_snapshot.go`, generated bindings, `docs/adr/0003-policy-key.md`, `docs/ARCHITECTURE.md` | **0** |
| MER-15 | **wire↔C equivalence tests + depguard wall** | Platform | 2h | T1 test asserts `wire.PolicyFlags` bit values ≡ generated `policy_verdict.flags` constants, `wire.PolicyRuleKey` fields map 1:1 (incl. direction) to the generated key, and `unsafe.Sizeof` matches the C static asserts; `.golangci.yml` rule added: only `internal/agent/datapath` may import both `pkg/wire` and `bpf`; `make lint` green | MER-14 | `internal/agent/datapath/translate_test.go` (new), `.golangci.yml` | 2 |
| MER-16 | **`tc_ingress.c`: parser + identity resolution** | eBPF | 3–4h | Program loads verifier-clean on 5.15 (T2 load test); parses eth/IPv4/L4 with the counter.c bounds discipline; looks up src+dst in `identity_map`; unknown-identity behavior follows ADR-0001 (incl. `FALLOPEN_UNKNOWN` toggle if the ADR chose it), covered by a T2 case per posture; non-IPv4 passes through | MER-14 | `bpf/tc_ingress.c` (new), `bpf/gen.go`, generated bindings | 1 |
| MER-17 | **Verdict enforcement + decision-point emission (D13)** | eBPF | 3–4h | `policy_map` lookup keyed per D12; ALLOW→`TC_ACT_OK`, DENY→`TC_ACT_SHOT` + `denied_flows_map` upsert + event, REDIRECT→mark-only placeholder + event; `flow_event` emitted **only** on connection-open (TCP SYN / first-sight UDP), DENY, REDIRECT — a 100-packet allowed flow produces exactly 1 event (T2 asserted); metric slots 2–5 increment correctly; `counter.c` demoted to toolchain-test-only (comment + Makefile/docs note) | MER-16 | `bpf/tc_ingress.c`, `bpf/counter.c`, generated bindings | 2 |
| MER-18 | **P1.1 GATE — T2 verdict matrix ≡ reference evaluator** | eBPF + Platform (pair) | 3–4h | Table-driven `prog_test_run` suite covering allow / deny / redirect / unknown-identity (both postures) / malformed / IPv6-passthrough; every case's kernel verdict equals `reference.Evaluator` output for the same tuple — zero hand-written expected values; synth-packet builders extracted to a shared helper; suite green in CI | MER-17, MER-22 | `test/bpf/verdict_test.go` (new), `test/bpf/packets.go` (new, extracted from progrun_test.go) | 4 |
| MER-19 | **Parser negative/regression T2 suite** (review T-3/T-4) | eBPF (delegable) | 2–3h | T2 cases: truncated eth, truncated IPv4, `ihl<5`, `ihl>15`, VLAN-tagged, non-IPv4 — each returns `TC_ACT_OK`, increments `METRIC_PACKETS_TOTAL`, emits no event; one byte-level test reads a ring record after `prog.Run` and asserts every field incl. zeroed pads (offsets 22, 36, 50–55) | none (starts vs `counter.c`; re-pointed at `tc_ingress` after MER-17) | `test/bpf/malformed_test.go` (new), `test/bpf/ringbytes_test.go` (new) | 1 |
| MER-20 | **`tc_egress.c` + Geneve identity option push** | eBPF | 3–4h | Egress program loads verifier-clean; for tunnel-bound traffic, pushes a Geneve option whose 4-byte body = `src_identity` (CC-3) at the attach point ADR-0002 chose; T2 test asserts the rewritten packet bytes contain the option with correct class/type/body; non-tunnel traffic untouched | MER-17 | `bpf/tc_egress.c` (new), `bpf/gen.go`, `bpf/include/meridian_consts.h` (Geneve class/type consts), generated bindings | 3 |
| MER-21 | **P1.3 GATE — Geneve ingress parse + two-node test** | eBPF | 4h | `tc_ingress` uses the carried option's identity as `src_identity` when the src IP is not in `identity_map`; two-"node" netns test (MER-28 topology): flow from node-A workload arrives at node B with correct remote `src_identity` decoded AND policy keyed on that identity enforces (allow case + deny case both asserted) | MER-20, MER-28 | `bpf/tc_ingress.c`, generated bindings, `test/integration/geneve_test.go` (new) | 4 |
| MER-22 | **Reference evaluator implementation** | Platform | 3–4h | `reference.Evaluator` implemented pure-Go (stdlib + `pkg/wire` only — depguard already enforces); evaluates `(src,dst,port,proto,direction) → PolicyVerdict` incl. unknown-identity rules per ADR-0001; exhaustive table tests incl. ID-0 cases, flag combinations, and the sockmap invariant (`SOCKMAP_ELIGIBLE` ⇒ ALLOW ∧ ¬L7 ∧ ¬mTLS) rejected as invalid input | MER-14 | `internal/reference/evaluator.go` (new), `internal/reference/evaluator_test.go` (new) | 1 |
| MER-23 | **Policy compiler** | Platform | 4h | Declarative policy model defined in `pkg/wire` (rules referencing SPIFFE-named identities, port/proto selectors); `Compile` produces `[]wire.PolicyRule` with deterministic ordering (same input ⇒ byte-identical output, asserted); snapshot tests committed for ≥3 representative policy sets; invalid policies rejected with actionable errors | MER-22 | `internal/control/compiler.go` (new), `internal/control/compiler_test.go` (new), `pkg/wire/policy.go` (new) | 2 |
| MER-24 | **CP-2 GATE — compiler ≡ reference property harness** | Platform | 3–4h | Property test: random policy sets + random flow tuples → compiled-rules lookup verdict ≡ `reference.Evaluator` verdict; zero divergence over ≥1e6 tuples locally and in a nightly CI job; PR CI runs 1e4; failing seeds auto-logged and a regression corpus committed | MER-23 | `internal/control/conformance_test.go` (new), `.github/workflows/ci.yml` (nightly job) | 3 |
| MER-25 | **`attach.Manager` netlink implementation** | Agent | 3–4h | `attach.Manager` implemented with `vishvananda/netlink` (clsact qdisc replace, BPF filter add direct-action); program pin uses unpin-or-replace so agent restart never EEXISTs (review P-3, T2-tested); `meridian-agent --iface <veth>` attaches via this path (review F-3); `Detach` removes filter+qdisc idempotently; `EnsureAttached` twice is a no-op | none | `internal/agent/attach/manager_linux.go` (new), `internal/agent/attach/manager_test.go` (T2, new), `cmd/meridian-agent/main.go`, `go.mod`/`go.sum` | 1 |
| MER-26 | **`datapath.Writer` implementation** | Agent | 4h | `Apply(CommitPlan)` executes **adds/updates before deletes** (D5, ordering asserted via a recording fake in T1); sole wire→bpf2go translation lives in `translate.go` (D17); direction byte always set, never-zero-pad bug impossible by construction; re-applying the same plan is a no-op; partial-failure surfaces an error naming the failed key | MER-14 | `internal/agent/datapath/writer_linux.go` (new), `internal/agent/datapath/translate.go` (new), `internal/agent/datapath/writer_test.go` (T1 w/ fake maps, new) | 2 |
| MER-27 | **A-1 agent stub: YAML → snapshot → CommitPlan** | Agent | 3–4h | Static YAML (identities + rules) loads/validates into `wire.PolicySnapshot`; diff against current state produces a minimal `CommitPlan`; startup order is seed-maps-then-attach (D16, asserted in T3 via MER-29); agent restart against existing pins re-opens (schema v2 sentinel) and converges without wiping; malformed YAML fails fast with line context | MER-26 | `internal/agent/config/yaml.go` (new), `internal/agent/config/yaml_test.go` (new), `cmd/meridian-agent/main.go`, `internal/agent/supervisor/runner.go` (minimal, new), `test/integration/testdata/policy.yaml` (new) | 3 |
| MER-28 | **Harness growth: two-node topology + seeding + traffic helpers** | Agent (delegable) | 4h | `TwoNode` fixture: two veth pairs + routing with a Geneve link between "nodes" (per ADR-0002), reaper-covered, per-run namespaced; `SeedIdentity`/`SeedPolicy` helpers write maps via `datapath.Writer` (not raw map ops); `AssertAllowed`/`AssertDenied` nc-based helpers with `WaitUntil` (no sleeps); fixture self-test passes T3 | none | `test/harness/twonode.go` (new), `test/harness/seed.go` (new), `test/harness/netns.go` | 1 |
| MER-29 | **P1.2 GATE — live policy integration test** | Agent | 4h | One T3 test, full product path (agent stub, not raw harness ops): allowed `nc` connect succeeds; denied connect is dropped (timeout, not refused); flipping one `policy_map` entry allow→deny takes effect on the next connection with no detach; `denied_flows_map` contains the denied flow with correct key + reason; agent kill + restart preserves enforcement (pins survive) | MER-17, MER-25, MER-26, MER-27, MER-28 | `test/integration/policy_test.go` (new) | 4 |
| MER-30 | **Metrics reader + Prometheus endpoint** | Obs | 3h | `internal/agent/metrics` exposes per-CPU summed reads of `metrics_map` by metric ID; both test-suite `sumPercpu` copies deleted in favor of it (review D-3); `/metrics` on `:9901` serves the counters via `prometheus/client_golang`; T1 tests against a fake reader; T2 test against a live map | none | `internal/agent/metrics/reader_linux.go` (new), `internal/agent/metrics/prometheus.go` (new), `internal/agent/metrics/reader_test.go` (new), `go.mod`/`go.sum`, `test/bpf/progrun_test.go`, `test/integration/counter_test.go` | 1 |
| MER-31 | **Flow aggregation + identity name resolution** | Obs | 3–4h | Ring events aggregate into allow/deny/bytes counters keyed by `(src_identity, dst_identity)`; `identitytable.Resolver` implemented over the stub YAML identities; Prometheus labels carry SPIFFE names (unknown ⇒ `"unknown"`, never empty); aggregation is bounded (LRU or cap) — no unbounded label cardinality; T1-tested with the `ExampleProducer` | MER-14, MER-30 | `internal/agent/telemetry/aggregate.go` (new), `internal/agent/telemetry/aggregate_test.go` (new), `internal/agent/identitytable/yaml.go` (new) | 2 |
| MER-32 | **O-2 GATE — denied-flows join + metrics assertion** | Obs | 3h | `denied_flows_map` reader joins drop entries with resolved identity names; extension of the MER-29 scenario curls `:9901/metrics` and asserts exact allow/deny counter values for the generated traffic; denied flows in the output carry SPIFFE names | MER-29, MER-31 | `internal/agent/metrics/denied.go` (new), `test/integration/metrics_test.go` (new) | 5 |
| MER-33 | **Schema version single-sourcing** (review D-1) | Agent or Obs | 2h | `MERIDIAN_SCHEMA_VERSION` exported through bpf2go (`enum meridian_schema` in `-type`); the hand-written Go literal in the loader deleted; T2 test: stamp sentinel with v1, reload with v2 build, require the fail-closed error (review T-2) | MER-14 | `bpf/include/meridian_types.h`, `bpf/gen.go`, generated bindings, `internal/agent/bpfobj/loader_linux.go`, `internal/agent/bpfobj/loader_test.go` (T2, new) | 2 |
| MER-34 | **EXIT GATE — ADR-0004 map-schema freeze + doc reconciliation** | eBPF + all (review) | 2h | ADR-0004 freezes every v2 map schema and cross-boundary struct (kernel half of CC-2) with the bump procedure; D12–D17 recorded as-built in ARCHITECTURE.md (deviations noted, not papered over); README/PHASE0_CHECKLIST/plan docs updated; all five other gates referenced as green with CI run links | MER-18, MER-21, MER-24, MER-29, MER-32 | `docs/adr/0004-map-schema-freeze.md` (new), `docs/ARCHITECTURE.md`, `README.md`, `PHASE0_CHECKLIST.md` | 6 |

## Dependency graph / schedule

```text
Wave 0 (serialization point): MER-14
  pre-startable with zero deps: MER-19 ∥ MER-25 ∥ MER-28 ∥ MER-30

Lane eBPF:     MER-14 → MER-16 → MER-17 → MER-20 → MER-21*   (+MER-18*, MER-19)
Lane Platform: MER-14 → MER-22 → MER-23 → MER-24*            (+MER-15, MER-18* pair)
Lane Agent:    MER-25 ∥ MER-28 · MER-14 → MER-26 → MER-27 → MER-29*  (+MER-33)
Lane Obs:      MER-30 · MER-14 → MER-31 → MER-32*

Joins: MER-18 ← {17, 22} · MER-21 ← {20, 28} · MER-29 ← {17, 25, 26, 27, 28}
       MER-32 ← {29, 31} · MER-34 ← all five gates (*)
```

- **Critical path ≈ 21–24h**: MER-14 → MER-16 → MER-17 → MER-29 → MER-32 → MER-34.
- **Gates**: P1.1 = MER-18 · P1.2 = MER-29 · P1.3 = MER-21 · CP-2 = MER-24 ·
  O-2 = MER-32 · **Phase 1 exit / Phase 2 entry** = MER-34.
- **Schedule risk** sits in MER-17/MER-18 (verifier + verdict-matrix debugging) —
  same class as Phase 0's MER-6; the 4-person wave plan carries ~1.5 days of slack for it.
- Staffing variants (4/2/1 person) and the parallel-safety rules are in
  [PHASE1_PLAN.md §3](PHASE1_PLAN.md).
