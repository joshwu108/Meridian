# Phase 0 Implementation Checklist

Maps the PRD Phase 0 deliverables and the ROADMAP week-1 exit gates onto
concrete steps. Items are ordered — each unblocks the next. Scaffolding
(everything marked ✅) was generated by the Phase 0 design round; the ⬜ items
require a Linux 5.15 environment.

## Done (scaffolding in this commit)

- ✅ Repository structure: `bpf/`, `cmd/meridian-agent/`, `internal/agent/{bpfobj,telemetry}/`, `pkg/wire/`, `test/{harness,bpf,integration,vm}/`
- ✅ Frozen cross-boundary headers: `bpf/include/meridian_types.h` (56-byte `flow_event` + static assert, metric enum, schema version), `meridian_maps.h`, `meridian_consts.h`
- ✅ Phase 0 TC program `bpf/counter.c` (bounds-checked eth+IPv4+L4 parse, PERCPU counter, per-packet ring emission with `TODO(phase1)` marker)
- ✅ `bpf/gen.go` bpf2go directives (`Counter` object, `-type flow_event`)
- ✅ `internal/agent/bpfobj`: load-or-reopen loader with schema-sentinel stamp/verify (fail closed on mismatch)
- ✅ `internal/agent/telemetry`: ring consumer (clean Close-based shutdown, drop accounting, boot-offset wall-clock conversion) + portable unit tests
- ✅ `cmd/meridian-agent`: Phase 0 binary (load, optional tc attach, ring tail)
- ✅ Test harness: per-run netns/bpffs namespacing, reaper, `WaitUntil` (no sleeps), `VethPair` fixture
- ✅ T2 test (`bpf_prog_test_run` synthetic packet → verdict + counter) and T3 test (veth + ping → counter + decoded events via the real consumer)
- ✅ `Makefile` (ebpf/vmlinux/verify-gen/build/test tiers/doctor/test-clean), `go.mod` (minimal, later deps pinned in comments), `.gitignore`, `.golangci.yml` (depguard import rules), CI workflow, Lima VM config + provisioning
- ✅ `docs/ARCHITECTURE.md` (merged four-workstream design: TC ingress, frozen map schema, agent internals, test environment)

## To do — first Linux session (Lima VM or any Ubuntu 22.04 box)

1. ⬜ `limactl start --name=meridian test/vm/meridian.yaml` (or provision a 5.15 box with `test/vm/provision.sh`)
2. ⬜ `make doctor` — all kernel config checks green
3. ⬜ `go mod tidy` — resolve `go.sum` (network required; verify cilium/ebpf v0.17.x + x/sys pins)
4. ⬜ `make vmlinux` — generate and **commit** `bpf/include/vmlinux.h`
5. ⬜ `make ebpf` — first bpf2go run; **commit** `bpf/counter_bpfel.go` + `bpf/counter_bpfel.o`.
   Verify the generated identifiers match the Go code's assumptions:
   `bpf.CounterObjects{MeridianCounter, FlowEvents, MetricsMap, SchemaSentinelMap}` and
   `bpf.CounterFlowEvent{TimestampNs, SrcIp, DstIp, SrcPort, DstPort, Proto, Verdict, SrcIdentity, DstIdentity, Bytes, LatencyNs, L7StatusCode}`
   — adjust `consumer_linux.go` / `loader_linux.go` / tests if bpf2go's casing differs.
6. ⬜ `go build ./...` and `make build` — everything compiles on Linux
7. ⬜ `make test-unit` — T1 green (also run on the macOS host: portable packages must pass there too)
8. ⬜ `make test-bpf` — **gate P0.1/P1-prep:** program loads verifier-clean; synthetic packet returns `TC_ACT_OK` and increments the counter
9. ⬜ `make test-integration` — **gates P0.2 + O-1:** ping traffic counted in the PERCPU map AND decoded by the real ring consumer with correct addresses
10. ⬜ `make verify-gen` — run twice; second run must produce zero diff (determinism gate)
11. ⬜ Push and confirm all three CI jobs green (CI cannot pass before steps 4–5 are committed)

## Phase 0 exit criteria (from ROADMAP week 1)

- [ ] `make ebpf` deterministic (CI `verify-gen` green)
- [ ] Counter program loads and attaches verifier-clean on 5.15
- [ ] Ring-buffer record decodes byte-correct in Go (T3 assertion 2)
- [ ] PERCPU counter readback matches injected traffic (T2 + T3 assertion 1)

## Decisions to make before Phase 1 (each gets an ADR in docs/adr/)

- [ ] **Unknown-identity posture** (CC-5): fail-open passthrough (PRD skeleton) vs default-deny-on-attach. Recommendation: default-deny once the agent populates `identity_map` before TC attach.
- [ ] **Geneve parse placement** relative to kernel decap (underlay-iface pre-decap vs tunnel-device program) — the largest unresolved data-path detail; affects agent attach topology.
- [ ] **Module path**: `github.com/joshuawu/meridian` is a placeholder (no remote exists). Changing later is a tree-wide rewrite — decide before Phase 1 code lands.
- [ ] **License split**: `bpf/` must be GPL-2.0 (GPL-only helpers); Go code license (Apache-2.0 proposed) is a legal decision — no LICENSE file was generated.
- [ ] **bpf2go prefix convention** for Phase 1: per-program objects (current) vs one combined collection once programs share maps. Recommendation: one combined collection at Phase 1 schema freeze.

## Deliberately deferred / omitted

- `internal/agent/{datapath,attach,linkwatch,xds,svid,...}`, `internal/control/`, `internal/proxy/`, `internal/reference/` — created by the phase that first needs them (no empty-directory litter).
- `CODEOWNERS` — single-contributor repo; the directory→subsystem ownership map lives in the Agent 4 design output and can be materialized when there's a team.
- Per-packet ring emission in `counter.c` — intentional for Phase 0 validation; flips to decision-point emission in Phase 1 (`TODO(phase1)` marker in the source).
- `test/integration` Geneve/TPROXY/multi-node fixtures — Phase 1+; the `VethPair` fixture is the seed.
